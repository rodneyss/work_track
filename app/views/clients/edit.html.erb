<h1>Editing Client</h1>

<%= render 'form' %>

<div id="bottomMenu">
<%= link_to '<div class="bottomMenu">Back</div>'.html_safe, @client %>
 <%= link_to '<div class="bottomMenu">Destroy</div>'.html_safe, @client, method: :delete, data: { confirm: 'Are you sure?' } %>
</div>